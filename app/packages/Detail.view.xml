<mvc:View
    height="100%"
    controllerName="packages.Detail"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:l="sap.ui.layout"
    xmlns:suite="sap.suite.ui.microchart"
    xmlns:macros="sap.fe.macros">
<Page >
    <Table width="100%" binding="{pkg>/}" growing="true" items="{pkg>IntegrationDesigntimeArtifacts}">
        <headerToolbar>
            <Toolbar width="100%">
                <Label text="[OUR] IntegrationDesigntimeArtifacts ({pkg>IntegrationDesigntimeArtifactsCount})"/>
                <ToolbarSpacer/>
                <SearchField width="20rem" liveChange="onSearch"/>
                <Button icon="sap-icon://chain-link" press="gotoPackage"/>
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Label text="{i18n>ID}"/>
            </Column>
            <Column>
                <Label text="{i18n>DESCRIPTION}"/>
            </Column>
            <Column>
                <Label text="{i18n>TYPE}"/>
            </Column>
            <Column>
                <Label text="{i18n>STATUS}"/>
            </Column>
            <Column>
                <Label text="{i18n>VERSION}"/>
            </Column>
            <Column>
                <Label text="{i18n>ACTIONS}"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem vAlign="Middle">
                <cells>
                    <ObjectIdentifier text="{pkg>Id}" title="{pkg>Name}"/>
                    <Text text="{pkg>Description}"/>
                    <Text text="{pkg>Runtime/Type}"/>
                    <ObjectStatus text="{pkg>Runtime/Status}"/>
                    <Link text="{pkg>Version}"/>
                    <Button icon="sap-icon://action" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>

    <Table width="100%" binding="{pkg>/}" 
		items="{
			path : 'pkg>IntegrationRuntimeArtifacts',
			filters:[{path:'Type', operator: 'NE', value1: 'INTEGRATION_FLOW'}],
			sorter : { path : 'DeployedOn', descending : true }
		}">
        <headerToolbar>
            <Toolbar width="100%">
                <Label text="[ALL] IntegrationRuntimeArtifacts"/>
                <ToolbarSpacer/>
                <SearchField width="20rem" liveChange="onSearch"/>
                <!-- <Button icon="sap-icon://sort" id="workspaceSortColumns"/> -->
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Label text="{i18n>ID}"/>
            </Column>
            <Column>
                <Label text="{i18n>DEPLOYED}"/>
            </Column>
            <Column>
                <Label text="{i18n>TYPE}"/>
            </Column>
            <Column>
                <Label text="{i18n>STATUS}"/>
            </Column>
            <Column>
                <Label text="{i18n>VERSION}"/>
            </Column>
            <Column>
                <Label text="{i18n>ACTIONS}"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem vAlign="Middle">
                <cells>
                    <ObjectIdentifier text="{pkg>Id}" title="{pkg>Name}"/>
                    <Text text="{pkg>DeployedOn} {pkg>DeployedBy}"/>
                    <Text text="{pkg>Type}"/>
                    <ObjectStatus text="{pkg>Status}"/>
                    <Link text="{pkg>Version}"/>
                    <Button icon="sap-icon://action" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</Page>
</mvc:View>